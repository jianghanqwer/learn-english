services:
  client:
    build: ./client
    ports:
      - "8010:80"
    depends_on:
      - server
    restart: unless-stopped

  server:
    build: ./server
    ports:
      - "3005:3001"
    env_file:
      - ./server/.env
    restart: unless-stopped
